const express = require('express')

const parentCategory = express.Router()

parentCategory.get('/',)
parentCategory.get('/:id',)
parentCategory.delete('/:id',)
parentCategory.post('/',)

// parentCategory.put('/:id')

module.exports = parentCategory